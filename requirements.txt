Flask==3.1.*
# Imported directly (secure_filename is load-bearing security code), so pinned
# here rather than left to whatever Flask resolves.
Werkzeug==3.1.*
pymongo[srv]==4.12.*
python-dotenv==1.1.*
bcrypt==4.3.*
requests==2.32.*
